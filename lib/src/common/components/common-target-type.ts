export enum CommonTargets {
  PREPARE = 'prepare',
  BUILD = 'build',
  INSTALL = 'install',
  COMPILE = 'compile',
  PACKAGE = 'package',
  LINT = 'lint',
  LINT_FIX = 'lint-fix',
  TEST = 'test',
  RELEASE = 'release',
  DEPLOY = 'deploy',
  PUBLISH = 'publish',
}
