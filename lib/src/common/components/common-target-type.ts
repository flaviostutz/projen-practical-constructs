export enum CommonTargets {
  BUILD = 'build',
  INSTALL = 'install',
  COMPILE = 'compile',
  PACKAGE = 'package',
  LINT = 'lint',
  LINT_FIX = 'lint-fix',
  TEST = 'test',
  RELEASE = 'release',
  DEPLOY = 'deploy',
  PUBLISH = 'publish',
}
